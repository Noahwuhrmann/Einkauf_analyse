#!/usr/bin/env python3# -*- coding: utf-8 -*-"""Created on Wed Dec  4 01:13:05 2024@author: noahwuhrmann"""''' Das wird das Programm für die Einkaufsanalyse für die Frima Taurus Sports AG'''import streamlit as stimport pandas as pddef check_excel_file(file):    """Prüft, ob die hochgeladene Excel-Datei in Ordnung ist."""    try:        # Datei laden        data = pd.read_excel(file)        # Überprüfen, ob wichtige Spalten vorhanden sind        required_columns = ["Artikelname", "Verkäufe", "Bestand"]        missing_columns = [col for col in required_columns if col not in data.columns]        if missing_columns:            return f"Die Excel-Datei fehlt folgende Spalten: {', '.join(missing_columns)}"                # Überprüfen, ob es leere Werte gibt        if data.isnull().values.any():            return "Die Excel-Datei enthält leere Werte. Bitte überprüfen."        # Datei ist in Ordnung        return "Die Excel-Datei ist in Ordnung!"    except Exception as e:        return f"Fehler beim Verarbeiten der Excel-Datei: {e}"# Streamlit-Web-Appst.title("Einkauf Analyse - Excel Checker")st.write("Lade eine Excel-Datei hoch, um zu überprüfen, ob sie korrekt formatiert ist.")# Datei hochladenuploaded_file = st.file_uploader("Excel-Datei hochladen", type=["xlsx"])if uploaded_file is not None:    st.write("Datei wurde hochgeladen!")    result = check_excel_file(uploaded_file)    st.write(result)